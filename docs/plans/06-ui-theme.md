# Plan 06: UI/UX 테마 & 디자인 시스템

## 1. 개요

본 문서는 **"창원원룸 청소 예약 시스템"**의 시각적 정체성과 일관된 사용자 경험을 위한 디자인 가이드라인입니다.

청소 서비스의 핵심 가치인 **"청결함"**, **"신뢰"**, **"간편함"**을 시각적으로 전달하여 사용자가 안심하고 서비스를 이용할 수 있도록 합니다.

---

## 2. 디자인 원칙 (Design Principles)

### 2.1 Clean & Fresh (깔끔함)
- 불필요한 장식을 배제하고 **여백을 충분히** 활용
- 흰색과 밝은 톤을 기반으로 **깨끗하고 산뜻한** 느낌
- 시각적 노이즈를 줄여 핵심 정보에 집중

### 2.2 Trustworthy (신뢰감)
- **차분하고 안정적인** 컬러 팔레트 사용
- 명확한 정보 계층 구조로 **투명성** 전달
- 일관된 UI 패턴으로 **예측 가능성** 제공

### 2.3 Accessible (접근성)
- WCAG 2.1 AA 기준 충족하는 **명암비** 확보
- 충분한 터치 타겟 크기 (최소 44px)
- 명확한 폰트 크기와 가독성 높은 타이포그래피

---

## 3. 컬러 시스템 (Color System)

Tailwind CSS 기본 팔레트를 기반으로, 브랜드에 맞게 확장하여 사용합니다.

### 3.1 Primary (브랜드 컬러) - 신뢰와 전문성

청결함과 신뢰를 상징하는 **스카이 블루** 계열을 메인으로 사용합니다.

| 톤 | 컬러명 | HEX | Tailwind | 용도 |
|---|--------|-----|----------|------|
| Lighter | Sky 50 | `#F0F9FF` | `sky-50` | 매우 연한 배경 |
| Light | Sky 100 | `#E0F2FE` | `sky-100` | 호버 배경, 선택 상태 |
| Base | **Sky 500** | `#0EA5E9` | `sky-500` | **메인 버튼, 활성 상태, 링크** |
| Dark | Sky 600 | `#0284C7` | `sky-600` | 버튼 호버, 강조 |
| Darker | Sky 700 | `#0369A1` | `sky-700` | 버튼 pressed |

```css
/* tailwind.config.js */
colors: {
  primary: {
    50: '#F0F9FF',
    100: '#E0F2FE',
    500: '#0EA5E9',
    600: '#0284C7',
    700: '#0369A1',
  }
}
```

### 3.2 Secondary (보조 컬러) - 신선함과 완료

| 톤 | 컬러명 | HEX | Tailwind | 용도 |
|---|--------|-----|----------|------|
| Light | Teal 100 | `#CCFBF1` | `teal-100` | 성공 배경 |
| Base | **Teal 500** | `#14B8A6` | `teal-500` | 완료 배지, 포인트 아이콘 |
| Dark | Teal 600 | `#0D9488` | `teal-600` | 완료 텍스트 강조 |

### 3.3 Neutrals (무채색) - 가독성

| 톤 | 컬러명 | HEX | Tailwind | 용도 |
|---|--------|-----|----------|------|
| 900 | Slate 900 | `#0F172A` | `slate-900` | 메인 타이틀 |
| 700 | Slate 700 | `#334155` | `slate-700` | 본문 텍스트 |
| 500 | Slate 500 | `#64748B` | `slate-500` | 부가 설명 |
| 400 | Slate 400 | `#94A3B8` | `slate-400` | 플레이스홀더 |
| 300 | Slate 300 | `#CBD5E1` | `slate-300` | 비활성 보더 |
| 200 | **Slate 200** | `#E2E8F0` | `slate-200` | **구분선, 보더** |
| 100 | Slate 100 | `#F1F5F9` | `slate-100` | 연한 배경 |
| 50 | **Slate 50** | `#F8FAFC` | `slate-50` | **앱 기본 배경** |

### 3.4 Semantic (상태 컬러)

| 상태 | 컬러명 | HEX | Tailwind | 용도 |
|------|--------|-----|----------|------|
| ✅ Success | Green 500 | `#22C55E` | `green-500` | 예약 확정, 청소 완료 |
| ⚠️ Warning | Amber 500 | `#F59E0B` | `amber-500` | 주의, 미확인 알림 |
| ❌ Error | Red 500 | `#EF4444` | `red-500` | 예약 취소, 오류 |
| ℹ️ Info | Sky 500 | `#0EA5E9` | `sky-500` | 안내, 정보 |

### 3.5 컬러 조합 예시

```
[화면 구성]
┌─────────────────────────────────────────┐
│ bg-slate-50 (앱 배경)                    │
│  ┌───────────────────────────────────┐  │
│  │ bg-white (카드)                    │  │
│  │ text-slate-900 (제목)              │  │
│  │ text-slate-500 (설명)              │  │
│  │ ┌─────────────────────────────┐   │  │
│  │ │ bg-primary-500 (버튼)        │   │  │
│  │ │ text-white (버튼 텍스트)      │   │  │
│  │ └─────────────────────────────┘   │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

---

## 4. 타이포그래피 (Typography)

### 4.1 폰트 패밀리

| 언어 | 폰트 | 비고 |
|------|------|------|
| 한글 | **Pretendard** | 가독성 높은 모던한 산세리프 |
| 영문 | **Inter** | 숫자, 영문 표현에 적합 |
| 아이콘 | **Lucide** | 선형 아이콘, 2px stroke |

```css
/* tailwind.config.js */
fontFamily: {
  sans: ['Pretendard', 'Inter', 'system-ui', 'sans-serif'],
}
```

### 4.2 타이포그래프 스케일

| 스타일 | 크기 | 굵기 | Tailwind | 용도 |
|--------|------|------|----------|------|
| **H1** | 24px | Bold (700) | `text-2xl font-bold` | 메인 화면 헤더 |
| **H2** | 20px | Bold (700) | `text-xl font-bold` | 섹션 타이틀 |
| **H3** | 18px | Semibold (600) | `text-lg font-semibold` | 카드 타이틀 |
| **Body 1** | 16px | Medium (500) | `text-base font-medium` | 본문 기본 |
| **Body 2** | 14px | Regular (400) | `text-sm font-normal` | 부가 정보, 리스트 |
| **Caption** | 12px | Regular (400) | `text-xs font-normal` | 설명, 날짜, 태그 |
| **Button** | 16px | Semibold (600) | `text-base font-semibold` | 버튼 텍스트 |

### 4.3 라인 높이

```css
/* 가독성을 위한 line-height */
leading-tight  /* 1.25 - 제목용 */
leading-normal /* 1.5 - 본문용 */
leading-relaxed/* 1.625 - 긴 텍스트 */
```

---

## 5. UI 컴포넌트 스타일

### 5.1 버튼 (Buttons)

#### Primary Button (주요 액션)
```tsx
// 예약하기, 확인, 다음
className="h-12 px-6 bg-primary-500 hover:bg-primary-600 
           active:bg-primary-700 text-white font-semibold 
           rounded-xl shadow-sm transition-all duration-200"
```

#### Secondary Button (보조 액션)
```tsx
// 취소, 이전, 닫기
className="h-12 px-6 bg-white hover:bg-slate-50 
           active:bg-slate-100 text-slate-700 font-semibold 
           rounded-xl border border-slate-200 transition-all duration-200"
```

#### Ghost Button (텍스트 버튼)
```tsx
// 더보기, 링크
className="h-10 px-4 text-primary-500 hover:text-primary-600 
           hover:bg-primary-50 font-medium rounded-lg transition-colors"
```

#### 버튼 상태
| 상태 | 스타일 변화 |
|------|-----------|
| Default | 기본 스타일 |
| Hover | 배경색 어둡게, `shadow-md` 추가 |
| Pressed | 더 어둡게, 살짝 축소 (`scale-[0.98]`) |
| Disabled | `opacity-50`, `cursor-not-allowed` |
| Loading | 스피너 표시, 텍스트 숨김 |

### 5.2 카드 (Cards)

```tsx
// 기본 카드
className="bg-white rounded-2xl p-4 shadow-sm 
           border border-slate-100 hover:shadow-md 
           transition-shadow duration-200"

// 강조 카드 (선택됨)
className="bg-white rounded-2xl p-4 shadow-sm 
           border-2 border-primary-500"

// 예약 카드
┌──────────────────────────────────────┐
│ 📅 2024년 2월 15일 (목)              │
│ ───────────────────────────────────  │
│ 🏠 창원시 의창구 ○○아파트...          │
│ 🕐 오전 10:00 - 오후 1:00            │
│ ───────────────────────────────────  │
│ 상태: 예약 확정  ✓                    │
│ ┌─────────┐  ┌─────────┐            │
│ │  취소   │  │ 상세보기 │            │
│ └─────────┘  └─────────┘            │
└──────────────────────────────────────┘
```

### 5.3 입력 필드 (Inputs)

```tsx
// 기본 입력창
className="h-12 px-4 bg-slate-50 border border-slate-200 
           rounded-xl text-slate-900 placeholder:text-slate-400
           focus:bg-white focus:border-primary-500 focus:ring-2 
           focus:ring-primary-100 transition-all duration-200"

// 에러 상태
className="... border-red-500 focus:border-red-500 focus:ring-red-100"

// 라벨과 함께
<div className="space-y-1.5">
  <label className="text-sm font-medium text-slate-700">이름</label>
  <input className="..." />
  <p className="text-xs text-red-500">이름을 입력해주세요</p>
</div>
```

### 5.4 배지 (Badges)

```tsx
// 상태 배지
// 예약 확정
className="px-2.5 py-1 bg-green-100 text-green-700 
           text-xs font-medium rounded-full"

// 대기중
className="px-2.5 py-1 bg-amber-100 text-amber-700 
           text-xs font-medium rounded-full"

// 취소됨
className="px-2.5 py-1 bg-slate-100 text-slate-600 
           text-xs font-medium rounded-full"
```

### 5.5 하단 네비게이션 (Bottom Navigation)

```tsx
// 모바일 하단 탭바
className="fixed bottom-0 left-0 right-0 h-16 
           bg-white border-t border-slate-200 
           flex items-center justify-around px-4"

// 아이템 (비활성)
className="flex flex-col items-center gap-1 
           text-slate-400 transition-colors"

// 아이템 (활성)
className="flex flex-col items-center gap-1 
           text-primary-500"
```

---

## 6. 레이아웃 & 스페이싱

### 6.1 8pt 그리드 시스템

모든 간격은 **8의 배수**를 기준으로 합니다.

| 이름 | 값 | Tailwind | 용도 |
|------|-----|----------|------|
| xs | 4px | `p-1`, `gap-1` | 아이콘과 텍스트 간격 |
| sm | 8px | `p-2`, `gap-2` | 요소 내부 최소 간격 |
| md | 16px | `p-4`, `gap-4` | **기본 패딩** |
| lg | 24px | `p-6`, `gap-6` | 섹션 내부 간격 |
| xl | 32px | `p-8`, `gap-8` | 섹션 간격 |
| 2xl | 48px | `p-12`, `gap-12` | 화면 경계 여백 |

### 6.2 컨테이너 너비

| 디바이스 | 최대 너비 | 패딩 |
|----------|----------|------|
| 모바일 | 100% | 16px |
| 태블릿 | 768px | 24px |
| 데스크톱 | 1024px | 32px |

### 6.3 안전 영역 (Safe Area)

```tsx
// iOS/Android 노치 대응
className="pt-safe-top pb-safe-bottom pl-safe-left pr-safe-right"
```

---

## 7. 아이콘 (Icons)

### 7.1 아이콘 라이브러리

**Lucide React / Lucide React Native** 사용

### 7.2 아이콘 크기

| 용도 | 크기 | Stroke Width |
|------|------|--------------|
| 네비게이션 | 24px | 2px |
| 버튼 내부 | 20px | 2px |
| 리스트 아이템 | 18px | 2px |
| 인라인 텍스트 | 16px | 2px |

### 7.3 자주 사용하는 아이콘

```
🏠 홈: Home
📅 예약: Calendar, CalendarDays
📋 예약목록: ClipboardList
👤 프로필: User, UserCircle
⚙️ 설정: Settings
🔍 검색: Search
📍 위치: MapPin
🕐 시간: Clock
⭐ 별점: Star, StarHalf
💬 리뷰: MessageSquare
📞 전화: Phone
✉️ 메시지: Mail
✅ 완료: Check, CheckCircle
❌ 취소/닫기: X, XCircle
⚠️ 주의: AlertTriangle
ℹ️ 정보: Info
➕ 추가: Plus
➖ 제거: Minus
```

---

## 8. 애니메이션 & 전환

### 8.1 기본 전환 (Transitions)

```css
/* 빠른 전환 - 버튼, 아이콘 */
transition-all duration-150

/* 기본 전환 - 카드, 모달 */
transition-all duration-200

/* 느린 전환 - 화면 전환 */
transition-all duration-300
```

### 8.2 이징 함수 (Easing)

```css
ease-default /* 기본 */
ease-in-out /* 부드러운 시작과 끝 */
ease-out /* 자연스러운 등장 */
```

### 8.3 애니메이션 예시

```tsx
// 버튼 클릭 피드백
className="active:scale-[0.98] transition-transform duration-100"

// 카드 호버
className="hover:shadow-md hover:-translate-y-0.5 transition-all duration-200"

// 모달 등장
className="animate-in fade-in slide-in-from-bottom-4 duration-200"

// 로딩 스피너
className="animate-spin"
```

---

## 9. 일러스트레이션 & 이미지 가이드

### 9.1 스타일 가이드

- **스타일**: 미니멀한 벡터 일러스트
- **컬러**: 브랜드 컬러(Sky, Teal) + 중성색(Slate)
- **분위기**: 밝고 긍정적이며 전문적인 느낌

### 9.2 사용 시나리오

| 위치 | 일러스트 종류 |
|------|--------------|
| 온보딩 | 청소 서비스 소개 (3-4장) |
| 빈 상태 | "예약 내역이 없습니다" |
| 에러 | "문제가 발생했습니다" |
| 성공 | "예약이 완료되었습니다" |

### 9.3 프로필 이미지

- 기본 아바타: 원형 + 이니셜 또는 아이콘
- 사진 업로드 시: 원형 크롭, 연한 보더

---

## 10. 다크 모드 (향후 확장)

초기에는 **Light Mode 우선** 지원하며, 다크 모드 확장이 용이하도록 설계합니다.

```css
/* CSS 변수 기반 컬러 (향후 다크 모드용) */
:root {
  --color-bg: theme('colors.slate.50');
  --color-surface: theme('colors.white');
  --color-text: theme('colors.slate.900');
}

.dark {
  --color-bg: theme('colors.slate.900');
  --color-surface: theme('colors.slate.800');
  --color-text: theme('colors.slate.100');
}
```

---

## 11. Tailwind 설정 예시

```js
// tailwind.config.js
module.exports = {
  content: ['./src/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#F0F9FF',
          100: '#E0F2FE',
          500: '#0EA5E9',
          600: '#0284C7',
          700: '#0369A1',
        },
        secondary: {
          100: '#CCFBF1',
          500: '#14B8A6',
          600: '#0D9488',
        },
      },
      fontFamily: {
        sans: ['Pretendard', 'Inter', 'system-ui', 'sans-serif'],
      },
      borderRadius: {
        'xl': '16px',
        '2xl': '20px',
      },
    },
  },
  plugins: [],
}
```

---

## 12. 적용 체크리스트

- [ ] Tailwind 커스텀 테마 설정
- [ ] Pretendard/Inter 폰트 로드
- [ ] 공통 Button 컴포넌트 구현
- [ ] 공통 Card 컴포넌트 구현
- [ ] 공통 Input 컴포넌트 구현
- [ ] Badge 컴포넌트 구현
- [ ] Navigation 컴포넌트 구현
- [ ] 아이콘 세트 선정 및 적용
- [ ] Storybook으로 컴포넌트 문서화 (선택)

---

**문서 버전**: v1.0  
**최종 수정**: 2026-02-11
